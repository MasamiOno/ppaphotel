<div class="row">
<div class="col-md-6 col-12 d-none d-md-block">

<% if user_signed_in? %>
<div class="page-header">
<%= search_form_for(@q, url: welcome_roomsearch_path, class: 'well') do |p| %>
<div class="form-group">
</div>
<%= p.submit 'オーナーさん所有の部屋を検索', class: 'btn btn-info'%>
</p>
<%= p.hidden_field :ownername_cont, class: 'form-control' ,value: current_user.name %>
<% end %>
</div>
</p>
</br>
<% end %>

<div class="page-header">
<h2>フロア案内</h2>
</div>
</p>
<div class="list-group">
<% @floors.each do |floor| %>
<%= link_to(floors_show_path(floor), class: 'list-group-item') do %>
<h5 class="list-group-item-heading">
<%= floor.floorname %></h5></p>
</p>
<p class="list-group-item-text">
</p>
<% end %>
<% end %>
</p>
</div>

<%= paginate @floors %>

</br>
</br>
<%= search_form_for(@q, url: welcome_roomsearch_path, class: 'well') do |f| %>
<div class="form-group">
<%= f.label :petname, 'ペット名で部屋を検索' %>
<%= f.text_field :petname_cont, class: 'form-control' , required:true %>
</div>
<%= f.submit '検索', class: 'btn btn-info' %>
<% end %>
