<h2>パスワード再作成メール送信</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-group">
    <%= f.label :'メールアドレス' %><br />
<%= f.email_field :email, autofocus: true, autocomplete: "email" ,class: "form-control col-md-5 col-sm" ,placeholder: "メールアドレス" %>
  </div>

  <div class="actions pb-5">
    <%= f.submit "パスワード再作成メールを送信" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</br>
</br>
